name: Production

on:
  push:
    branches: [ "production" ]
  pull_request:
    branches: [ "production" ]

jobs:
  Deploy:
    runs-on: [self-hosted]
    if: github.ref == 'refs/heads/production'
    steps:
      - name: Deploy
        run: |
          cd /home/khardl4test/public_html/
          ./deploy.sh